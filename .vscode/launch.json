{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Dipjo File",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}\\main.py",
            "args": ["${file}"],
            "console": "integratedTerminal"
        }
    ]
}
